# Production (VPS): points to the actual backend server.
# The frontend is served at sop.joshuageneration.com (port 80/443 via Nginx)
# and the backend runs on port 5001 on the same server.
# Nginx should proxy /api → http://localhost:5001, so we can use a relative path.
VITE_API_BASE_URL=/api
